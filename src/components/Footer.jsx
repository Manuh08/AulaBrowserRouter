export default function Footer({Creditos}) {
    return(
        <footer>
            <p>
            @ Feito Por {Creditos}. Todos os direitos são reservados
            </p>
        </footer>
    );
}